//
//  {{ prefix }}{{ module_info.name }}{{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import UIKit

class {{ module_info.name }}Assembly 
{
    class func createModule(output output: {{ module_info.name }}ModuleOutput?) -> {{ module_info.name }}ModuleInput 
    {
        let vc = initializeViewController()
        let interactor = {{ module_info.name }}Interactor()
        let presenter = {{ module_info.name }}Presenter()
        let router = {{ module_info.name }}Router()

        router.presenter = presenter
		router.viewController = vc

        vc.output = presenter

        interactor.output = presenter

        presenter.view = vc
        presenter.interactor = interactor
        presenter.router = router
		presenter.output = output

        return presenter
    }
	
	private class func initializeViewController() -> {{ module_info.name }}ViewController 
	{
	        let sb = UIStoryboard(name: "{{ module_info.name }}", bundle: nil)
	        let vc = sb.instantiateViewControllerWithIdentifier("{{ module_info.name }}ViewController") as! {{ module_info.name }}ViewController
    
	        return vc
	} 
}
